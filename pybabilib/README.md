# babilib

this is python library for controlling the babirl for CRIB experiment (mainly MPV)

## usage

package install

```sh
poetry install
poetry update
```

test code

```sh
poetry run pytest -v
```
